# Token de autenticación generado desde el backend
# Endpoint: POST /api/auth/mcp-token
MCP_API_KEY=tu_token_aqui

# URL del backend de la Plataforma de Acarreos
BACKEND_URL=http://localhost:3000

# Entorno (development | production)
NODE_ENV=development
